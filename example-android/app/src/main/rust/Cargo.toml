[package]
name = "example-android"
version = "0.0.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
dexkit = { path = "../../../../../dexkit" }
jni = "0.21"
android-logcat = "0.1"

[profile.release]
strip = true
lto = true
codegen-units = 1
opt-level = "z"
panic = "abort"